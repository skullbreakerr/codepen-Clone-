import React from 'react';

const GoogleIcon = ({iconName})=>{
  return(
    <div className="icon-container">
      <i className="material-icons">{iconName}</i>
    </div>
  );
}

export default GoogleIcon;